@model IEnumerable<DtoModel.NewsDto>

@{
    ViewBag.Title = "NewsIdex";
    Layout = null; 
}




<table class="table">
    <tr>
        <th>
            Nyheder
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
           <div class="textArea">@Html.DisplayFor(modelItem => item.News)</div>
        </td>
    </tr>
}

</table>
